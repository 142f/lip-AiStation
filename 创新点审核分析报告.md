# DP-TALT＋HS/ST-MIL 完整设计说明

## 一、设计依据与证据边界

本方案基于以下已确认内容：

* 现有论文方法为 **MB-ViT**：BGI负责全局音视频交互，RAE/SRAC负责三尺度、五帧嘴部区域建模；
* 当前局部分支已经得到五帧尺度融合特征，但随后直接进行算术平均；
* 当前全局分支将音频和视频patch token分别平均，输出窗口级全局特征；
* 当前训练目标为分类损失加0.01倍RAL；
* 论文采用5帧时间窗口，在AVLips训练，在FakeAVCeleb和LAV-DF上跨数据集测试；
* 论文已经明确指出，LAV-DF中的局部时间伪造可能在视频级均匀聚合中被稀释。 

当前代码审计显示，训练链为：

```text
融合音视频图
→ LipFD全局ViT
→ 全局音视频特征
→ 3尺度×5帧局部ResNet
→ 尺度融合
→ 5帧均值
→ 二分类
```

验证阶段和测试阶段还存在窗口级/视频级口径不一致、测试集选择阈值和聚合方式等问题，这些必须在正式创新实验前修复，否则新模块的增益无法可靠判断。

需要特别说明：目前可读取的完整仓库审计对应一个已记录的仓库快照，而最终开发目标是`main2-cx`。以下设计结合了此前已核实的`main2-cx`函数路径，但真正修改前仍应对实际分支重新执行一次函数签名和shape审计，避免分支差异导致设计与代码错位。

---

# 二、创新点总体描述

## 2.1 最终名称

# **局部音画转移对齐与稀疏伪造证据聚合**

由两个小创新点组成：

1. **DP-TALT：差异保持的时间锚定局部转移对齐**
   Difference-Preserving Timestamp-Anchored Local Transition Alignment。

2. **HS-TMIL / ST-MIL：层次化或窗口内稀疏转移多实例聚合**
   Hierarchical Sparse Transition Multiple-Instance Learning / Sparse Transition Multiple-Instance Learning。

最终采用HS-TMIL还是ST-MIL，由数据审计决定：

* AVLips训练集能够构造无泄漏的多窗口视频包时，采用HS-TMIL；
* 无法可靠恢复`video_id`或大多数视频只有一个窗口时，采用ST-MIL；
* 不允许在数据条件不满足时仍声称使用“层次化视频级MIL”。

---

## 2.2 总体目标

当前MB-ViT已经较好地解决了：

> 全局音视频特征与多尺度嘴部区域特征如何交互和融合。

第二大创新点进一步解决：

> 窗口内部哪一个局部音画变化存在异常，以及少量异常证据如何避免在五帧和视频均值聚合中被稀释。

完整逻辑为：

```text
第一大创新点MB-ViT
提取全局音视频特征和五帧多尺度嘴部特征
                        ↓
DP-TALT
显式建立音频变化与嘴部变化之间的局部对应关系
输出4个局部音画对齐差异token
                        ↓
ST-MIL
在窗口内从4个转移中保留稀疏异常证据
                        ↓
HS-TMIL（数据支持时）
在视频多个窗口中进一步保留少量异常窗口
                        ↓
最终视频级真假判断
```

两个小创新点是“证据构造—证据聚合”关系，而不是两个独立模块的堆叠。

---

# 三、总体目标与问题定义

## 3.1 当前模型的具体问题

### 问题一：五帧有顺序，但当前最终被直接平均

当前`region_awareness.py`在完成三个尺度的softmax融合后得到：

[
V_r,\qquad r=1,\ldots,5
]

其实际shape为：

```text
fused_parts: [5, B, C]
```

随后直接执行：

[
F_{\text{local}}
================

\frac{1}{5}\sum_{r=1}^{5}V_r
]

因此模型保留了“五个帧位置”，却没有显式利用：

* 第1帧到第2帧发生了什么变化；
* 哪一个相邻变化最异常；
* 异常是否只发生在少量时间位置。

当前代码的区域聚合确实是五区域算术平均，而不是时间级证据选择。

---

### 问题二：音视频只有窗口级融合，没有局部变化对应

当前`LipFD.get_features()`在Transformer之后，将音频patch token和视频patch token分别均值池化：

[
F_a=\operatorname{Mean}(\text{audio tokens})
]

[
F_v=\operatorname{Mean}(\text{video tokens})
]

再进行拼接、SE和投影。这样能够建模窗口总体音视频关系，但不能明确描述：

[
\text{第 }i\text{ 个嘴部变化}
\leftrightarrow
\text{第 }j\text{ 个音频变化}
]

---

### 问题三：稀疏伪造证据可能被两次稀释

当前可能发生两层平均：

```text
窗口内部：
5帧局部特征均值

视频内部：
多个窗口预测均值
```

若伪造只影响：

* 五帧中的一两个相邻转移；
* 长视频中的少量窗口；

异常分数可能被大量正常帧和正常窗口稀释。论文在LAV-DF上的结果和限制分析已经明确指出这一风险。MB-ViT在LAV-DF上的AUC/AP分别为72.19%和71.74%，明显弱于其在AVLips和FakeAVCeleb上的结果。

---

## 3.2 为什么该创新点有必要

现有论文结果说明：

* AVLips上的内部性能已经较高，继续增加普通通道注意力或尺度模块容易受到天花板效应影响；
* FakeAVCeleb跨域结果较好，说明现有全局—区域表示有一定泛化能力；
* LAV-DF中的局部短时伪造仍是主要薄弱点。

因此第二创新不应继续重复：

* SE；
* 多尺度；
* 普通帧差；
* 单纯音频门控；
* 另一个全局Transformer。

更合理的方向是使用现有五帧和音频patch，补上“局部对应”和“稀疏聚合”两个缺口。

---

## 3.3 预期改进目标

主要目标不是承诺AVLips大幅涨点，而是：

1. 改善LAV-DF局部或音视频联合操纵样本的排序质量；
2. 提高视频级AUC和AP；
3. 降低少量伪造窗口被均值稀释的概率；
4. 保持FakeAVCeleb跨域性能不发生系统性下降；
5. 降低不同随机种子下的结果波动；
6. 提供可解释的(4\times4)局部匹配场和转移实例分数；
7. 在不增加新主干和新标签的情况下完成。

上述目标目前均为：

> **尚待实验验证。**

---

# 四、整体架构与插入位置

## 4.1 应插入哪里

DP-TALT不应插入：

* ViT主干内部；
* ResNet主干内部；
* 原始输入预处理主路径；
* 最终视频预测完成之后。

推荐插入位置是：

> **局部分支完成三尺度融合之后、五帧平均之前。**

原因是此处已经具备：

* 五帧多尺度局部特征；
* 全局音视频上下文；
* 明确帧顺序；
* 尚未被时间平均的局部信息。

音频时间段辅助特征则从`LipFD.get_features()`内部提取，通过辅助返回值传给局部分支。

---

## 4.2 修改前的信息流

```text
Global image
    ↓
LipFD.get_features()
    ↓
F_global [B,Dg]

3 scales × 5 crops
    ↓
Shared ResNet
    ↓
F_local [S=3,R=5,B,Dl]
    ↓
拼接F_global、PE、SE
    ↓
尺度softmax
    ↓
V [R=5,B,C]
    ↓
五帧算术平均
    ↓
FC
    ↓
logits [B,2]
```

---

## 4.3 修改后的信息流

```text
Global image
    ↓
LipFD.get_features(return_temporal_aux=True)
    ├─ 原F_global [B,Dg]
    ├─ 5段局部音频特征 L_a [B,5,Da]
    └─ 深层全局音频语义 G_a [B,Dg_a]

3 scales × 5 crops
    ↓
Shared ResNet
    ↓
尺度融合后的五帧特征 V [B,5,C]
    ↓
DP-TALT
    ├─ 4个视觉转移 Z_v [B,4,d]
    ├─ 4个音频转移 Z_a [B,4,d]
    ├─ 匹配场 P [B,4,4]
    └─ 4个对齐差异token E [B,4,d]
    ↓
Transition Evidence Head
    ↓
4个实例logit s [B,4]
    ↓
ST-MIL LogSumExp
    ↓
窗口级稀疏证据 z_mil [B]
    ↓
与原始分类logits进行零初始化残差融合
    ↓
最终窗口logits [B,2]
    ↓
可选HS-TMIL视频级聚合
```

---

# 五、小创新点一：DP-TALT详细设计

## 5.1 输入

DP-TALT接收：

| 输入                 |      Shape | 来源                    |
| ------------------ | ---------: | --------------------- |
| `visual_regions`   | `[B,5,Cv]` | SRAC尺度融合后、五帧平均前       |
| `audio_segments`   | `[B,5,Ca]` | ViT浅层音频patch的时间分段     |
| `audio_global`     |   `[B,Cg]` | Transformer后音频token均值 |
| `valid_audio_mask` |    `[B,5]` | 可选，时间映射无覆盖时使用         |

---

## 5.2 音频五段提取

### 严格时间锚定模式

在进入Transformer之前，从二维patch embedding中保留音频patch网格：

[
E_a\in\mathbb R^{B\times H_a\times W_a\times C_a}
]

根据视觉帧时间区间 (I_r) 和音频patch时间区间 (J_k)，计算：

[
W_{rk}
======

\frac{|I_r\cap J_k|}{|J_k|}
]

音频段表示：

[
L_r^a
=====

\frac{\sum_k W_{rk}E_{a,k}}
{\sum_k W_{rk}+\epsilon}
]

严格模式要求数据或预处理配置能够恢复：

* 音频采样率；
* Mel谱`hop_length`；
* 频谱时间宽度；
* 视觉帧时间戳；
* patch kernel和stride；
* 音频在融合图中的位置。

当前数据接口主要返回图像、局部crop和标签，没有完整时间元数据，因此该模式暂时不能直接认定已经具备。

---

### 相对位置分组模式

若无法恢复真实时间戳，采用兼容模式：

```text
音频patch二维网格
→ 沿频率维池化
→ 沿时间位置划分5段
→ 加权平均得到5段表示
```

此时论文中应表述为：

> 相对位置分组的局部音频表示。

不能称为严格“时间戳锚定”。

---

### 必须完成的前置测试

1. 打印patch grid shape；
2. 标明哪个轴为频率、哪个轴为时间；
3. 检查五段覆盖权重行和；
4. 使用合成脉冲验证响应位置随时间移动；
5. 对原始音频做小幅shift，检查五段特征变化方向。

如果这些测试失败，必须降级为相对位置模式。

---

## 5.3 构造视觉转移

尺度融合后五帧表示：

[
V_1,\ldots,V_5
]

先投影到统一维度：

[
\bar V_r
========

\operatorname{LN}(W_vV_r)
]

构造保留方向的一阶转移：

[
D_i^v
=====

\bar V_{i+1}-\bar V_i,
\qquad i=1,\ldots,4
]

再进行归一化：

[
Z_i^v
=====

\operatorname{Normalize}
\left(
\phi_v(D_i^v)
\right)
]

首版不使用：

* 绝对值差分；
* 中心差分；
* 二阶差分；
* 时序Transformer。

原因是DP-TALT的创新核心不是差分算子，而是局部跨模态软匹配和高维传输。

---

## 5.4 构造音频转移

五段音频表示：

[
L_1^a,\ldots,L_5^a
]

局部音频转移：

[
D_i^a
=====

\phi_a(L_{i+1}^a-L_i^a)
]

为了利用预训练Transformer后的深层音频语义，又不让同一个全局向量淹没局部差异，只采用有界乘法调制：

[
M_i
===

1+
\alpha_s
\tanh
\left(
h[D_i^a,G_a]
\right)
]

[
Z_i^a
=====

\operatorname{Normalize}
\left(
\operatorname{LN}(M_i\odot D_i^a)
\right)
]

其中：

* (G_a)不直接加到五个音频段上；
* (M_i)只能增强或抑制已有局部变化；
* `semantic_modulation_scale`首版固定，不设置为自由无界参数。

建议MVP先保留配置开关：

```text
use_semantic_modulation=False
```

先验证纯局部音频转移有效，再开启语义调制。

---

## 5.5 构建局部匹配场

计算四个视觉转移与四个音频转移之间的余弦匹配：

[
S_{ij}
======

\frac{
(Z_i^v)^\top Z_j^a
}{
\tau_m
}
]

沿音频候选维归一化：

[
P_{ij}
======

\operatorname{Softmax}*j(S*{ij})
]

得到：

[
P\in\mathbb R^{B\times4\times4}
]

含义：

> 对于第 (i) 个视觉变化，四个音频变化分别具有多大的匹配权重。

首版使用row-wise softmax，不使用：

* Sinkhorn；
* 最优传输；
* 多轮迭代；
* 双向重建。

---

## 5.6 高维音频转移软传输

匹配场不直接展平为16维分类特征，而是控制高维音频变化传输：

[
\widetilde Z_i^a
================

\sum_{j=1}^{4}
P_{ij}Z_j^a
]

这样得到与每个视觉转移对应的音频转移表示。

---

## 5.7 构造对齐差异token

对于每一个视觉转移：

[
E_i
===

\psi
\left[
Z_i^v;
\widetilde Z_i^a;
Z_i^v-\widetilde Z_i^a;
Z_i^v\odot\widetilde Z_i^a
\right]
]

其中：

[
E_i\in\mathbb R^d
]

首版建议：

```text
transition_dim = 128
MLP: 4d → 2d → d
LayerNorm
GELU
Dropout默认为0或与原模型保持一致
```

输出保留四个实例：

```text
aligned_tokens: [B,4,d]
match_field:    [B,4,4]
```

不在DP-TALT内部提前平均。

---

# 六、小创新点二：ST-MIL与HS-TMIL详细设计

## 6.1 转移实例分数

对每个局部对齐差异token：

[
s_i
===

h_{\text{inst}}(E_i)
]

得到：

```text
instance_logits: [B,4]
```

这里的实例头仅是一个轻量MLP或线性层，不增加独立监督。

---

## 6.2 ST-MIL窗口内稀疏聚合

采用归一化LogSumExp：

[
z_{\text{mil}}
==============

\tau_t
\left[
\log
\sum_{i=1}^{4}
\exp(s_i/\tau_t)
----------------

\log4
\right]
]

它介于：

[
\operatorname{Mean}(s)
\quad\text{和}\quad
\operatorname{Max}(s)
]

之间。

### 首版设置

```text
tau_transition = 1.0
learnable_temperature = False
```

开发实验只在AVLips验证集搜索：

[
\tau_t\in{0.5,1.0,1.5}
]

一旦选定：

* 所有正式随机种子使用同一个值；
* FakeAVCeleb和LAV-DF不得重新选择；
* 不根据测试集结果修改。

---

## 6.3 与原分类logit融合

当前局部分支输出两类logit：

[
\mathbf l_{\text{base}}
=======================

[l_{\text{real}},l_{\text{fake}}]
]

为了让新增分支只改变真假margin，而不改变softmax无意义的公共偏置，定义：

[
\Delta
======

\alpha_{\text{mil}}z_{\text{mil}}
]

[
\mathbf l_{\text{mil}}
======================

\left[
-\frac{\Delta}{2},
+\frac{\Delta}{2}
\right]
]

最终：

[
\mathbf l_{\text{final}}
========================

\mathbf l_{\text{base}}
+
\mathbf l_{\text{mil}}
]

其中：

```python
alpha_mil = nn.Parameter(torch.tensor(0.0))
```

初始时：

[
\mathbf l_{\text{final}}
========================

\mathbf l_{\text{base}}
]

因此能够安全加载旧MB-ViT checkpoint。

需要把`alpha_mil`放入不使用weight decay的参数组，并监控：

* 参数值；
* 参数梯度；
* (\alpha_{\text{mil}}z_{\text{mil}})相对基础logit的贡献；
* 推理时关闭该分支后的指标变化。

不能只依据`alpha_mil`是否大于某个数判断模块生效。

---

## 6.4 HS-TMIL视频级聚合

只有数据审计通过时启用。

设视频包含 (N) 个窗口，每个窗口的真假margin为：

[
m_w
===

## l_{\text{fake},w}

l_{\text{real},w}
]

视频级聚合：

[
m_{\text{video}}
================

\tau_v
\left[
\log
\sum_{w=1}^{N}
\exp(m_w/\tau_v)
----------------

\log N
\right]
]

重构视频级两类logit：

[
\mathbf l_{\text{video}}
========================

\left[
-\frac{m_{\text{video}}}{2},
+\frac{m_{\text{video}}}{2}
\right]
]

然后使用视频标签计算CE。

首版固定：

```text
tau_video = 1.0
```

不同时搜索窗口温度和视频温度。

---

## 6.5 不使用的损失

最终主方案不加入：

* `L_real-inst`；
* 人工shift损失；
* 熵正则；
* 可靠性损失；
* RSEC；
* 对比损失；
* 实例级伪标签。

当前主损失保持：

[
L
=

L_{\mathrm{CE}}
+
0.01L_{\mathrm{RAL}}
]

与现有训练目标一致。当前代码确实使用FP32计算CE和RAL，并将RAL权重设为0.01。

---

# 七、训练阶段完整流程

## 7.1 ST-MIL训练流程

```text
1. Dataset读取融合图、15个crop和标签
2. LipFD输出：
   - F_global
   - audio_segments
   - audio_global
3. RegionAware提取3尺度×5帧局部特征
4. 按尺度权重融合，得到5帧V
5. 原路径：
   V均值 → base_logits
6. DP-TALT：
   V和audio_segments → 4个转移
   → 4×4匹配场
   → 4个aligned tokens
7. Instance Head：
   4个实例logit
8. ST-MIL：
   LogSumExp得到z_mil
9. 零初始化残差加入base_logits
10. CE + 0.01 RAL
11. 反向、optimizer、EMA
12. 视频级Validation AUC选择best
```

---

## 7.2 HS-TMIL训练流程

数据支持多窗口视频包时：

```text
1. VideoBagSampler采样Bv个视频
2. 每个视频采样K个窗口，产生[Bv,K,...]
3. 展平为[Bv×K,...]执行一次普通窗口前向
4. 得到[Bv,K,2]窗口logits
5. 转换为窗口真假margin
6. 对K个窗口执行视频级LogSumExp
7. 得到[Bv,2]视频logits
8. 使用视频标签计算CE
9. RAL在所有有效窗口上平均
```

公平比较时，应保持每次optimizer update处理的总窗口数接近基线，而不是同时保持相同视频数和窗口数。

需要警惕：

* 一个窗口含1120全局图和15个局部crop；
* 多窗口Bag会直接增加局部ResNet激活；
* 当前项目本身存在24 GB显存下的局部分支OOM风险。

因此HS-TMIL首版建议：

```text
bag_size = 2
总窗口数/optimizer update与基线一致
必要时减小video micro-batch并增加accumulation
```

---

# 八、推理阶段完整流程

## 8.1 窗口级推理

每个窗口输出：

* 基础真假logit；
* ST-MIL残差；
* 最终真假概率；
* 四个实例分数；
* (4\times4)匹配场；
* 可选健康指标。

---

## 8.2 视频级推理

根据验证集锁定的聚合方式：

```text
同一video_id的全部窗口
→ 固定mean或固定LogSumExp
→ 视频级score
→ AUC/AP
→ 使用验证集阈值计算ACC/FPR/FNR
```

测试阶段禁止：

* 重新搜索Youden阈值；
* 在多个聚合方式中挑最佳；
* 根据LAV-DF结果调整温度。

当前代码审计已经确认原测试流程同时计算多种聚合方式并在测试集选择阈值，因此评价协议修复是正式创新实验的前置条件。

---

# 九、创新性与可行性

## 9.1 相比原模型的实质差异

| 维度    | 原MB-ViT   | DP-TALT＋ST/HS-MIL |
| ----- | --------- | ----------------- |
| 音频表示  | 窗口级音频均值   | 五段局部音频＋四个转移       |
| 视觉时序  | 五帧最后均值    | 四个有方向的局部转移        |
| 音画关系  | 全局隐式交互    | 局部4×4显式软匹配        |
| 匹配结果  | 无局部匹配结构   | 高维音频转移软传输         |
| 局部证据  | 单个窗口表示    | 四个转移实例            |
| 窗口聚合  | 五帧均值      | 稀疏LogSumExp       |
| 视频聚合  | 统一均值      | 条件化视频级MIL         |
| 监督    | 窗口/视频真假   | 同一真假标签，无新标注       |
| 可解释输出 | 全局分数、尺度权重 | 匹配场＋实例分数          |

---

## 9.2 哪些属于已有技术

以下部分属于成熟基础算子，不应单独宣称创新：

* 一阶特征差分；
* 余弦相似度；
* softmax匹配；
* LogSumExp；
* 多实例学习；
* 残差连接；
* LayerNorm和MLP。

---

## 9.3 可主张的任务特定改进

可以作为论文贡献的组合是：

1. 在MB-ViT五帧多尺度嘴部表示上定义局部视觉转移；
2. 从早期融合音频区域中保留五段局部音频表示；
3. 建立四个音频转移与四个视觉转移的局部软匹配场；
4. 使用匹配场控制高维音频转移向视觉时间位置传输；
5. 将局部对齐差异作为多实例，而不是再次平均成单个向量；
6. 采用转移—窗口—视频的条件化稀疏聚合；
7. 保持现有分类损失和预训练主干不变。

创新性应定位为：

> **针对现有MB-ViT和局部唇同步伪造问题的结构化改进，而不是提出全新的差分、MIL或匹配理论。**

---

## 9.4 为什么可能有效

理论依据是：

* 局部伪造只影响少量时间位置时，均值会降低异常贡献；
* 显式音画转移匹配比单模态时序卷积更直接描述跨模态变化对应；
* LogSumExp能够提高高响应实例的贡献，同时比硬max更平滑；
* 模块使用现有特征，不需要新增大型编码器；
* 主分类损失直接监督最终检测，不依赖辅助任务是否与检测目标一致。

但上述推理并不等于实验结论。

---

## 9.5 计算复杂度

设：

* 局部输入维度为 (C_v)；
* 音频输入维度为 (C_a)；
* 统一转移维度为 (d)；
* batch size为 (B)。

主要新增计算：

| 模块          |                   复杂度 |
| ----------- | --------------------: |
| 视觉投影        |      (O(B\cdot5C_vd)) |
| 音频投影        |      (O(B\cdot5C_ad)) |
| 4×4匹配场      |        (O(B\cdot16d)) |
| 高维传输        |        (O(B\cdot16d)) |
| 差异token MLP | (O(B\cdot4\cdot4d^2)) |
| MIL聚合       |          (O(B\cdot4)) |

相对于ViT-L和15路局部ResNet前向，新增计算理论上较小，而且没有新增主干前向。不过参数量、FLOPs、峰值显存和单epoch时间必须在实现后实测，不能提前宣称低于某个固定百分比。

---

## 9.6 可能失败的原因

| 失败原因           | 表现                          | 简化方案                    |
| -------------- | --------------------------- | ----------------------- |
| 音频时间轴恢复错误      | shift后五段特征不移动               | 降级为相对位置分组               |
| 浅层音频缺少语义       | audio shuffle不影响结果          | 开启有界深层语义调制              |
| 4×4匹配场无结构      | 长期均匀或恒定                     | 改为同位置匹配或1D-CNN          |
| 转移表示坍缩         | 非对角余弦接近一致                   | 检查切片、广播、梯度和归一化          |
| MIL只选择噪声       | max接近1、Recall/FPR异常         | 增大固定温度或退回mean           |
| `alpha_mil`被忽略 | 关闭分支后预测不变                   | 检查梯度；必要时测试小非零初始化        |
| 视频级Bag不成立      | 单窗口比例高或video_id不可靠          | 使用ST-MIL                |
| 简单1D-CNN同样有效   | 性能和稳定性相同                    | 遵循奥卡姆剃刀，采用1D-CNN＋ST-MIL |
| 跨数据集下降         | AVLips涨、LAV-DF/FakeAVCeleb降 | 停止正式采用，检查源域过拟合          |

---

# 十、代码文件修改清单

## 10.1 必须修改

| 文件                           | 修改目的         | 主要内容                                     |
| ---------------------------- | ------------ | ---------------------------------------- |
| `models/LipFD.py`            | 暴露局部音频时间表示   | 提取浅层音频patch网格；可选返回5段音频和深层音频语义            |
| `models/region_awareness.py` | 在五帧平均前插入创新模块 | 保留`fused_parts`；调用DP-TALT和MIL；融合最终logits |
| `trainer/trainer.py`         | 接入辅助返回和健康日志  | 根据开关调用新路径；主损失保持不变；记录alpha、匹配场和梯度         |
| `options/train_options.py`   | 增加模块配置       | DP-TALT、MIL、温度、维度、日志和视频Bag开关             |
| `validate.py`                | 统一模型选择口径     | 显式video_id聚合；视频级AUC/AP选best；保存阈值         |
| `test.py`                    | 防止测试泄漏       | 固定聚合、温度和验证阈值；manifest优先                  |
| `data/datasets.py`           | 条件化返回元数据     | 可选返回sample_id、video_id、window_index和时间信息 |
| `models/__init__.py`         | 传递新配置        | 不新增不必要的模型名称，只把配置传入现有模型                   |

---

## 10.2 建议新增

```text
models/
├── local_transition_alignment.py
├── transition_mil.py
└── transition_outputs.py

utils/
├── transition_health.py
└── video_aggregation.py

tools/
├── audit_video_bags.py
├── build_window_manifest.py
└── inspect_audio_patch_mapping.py

tests/
├── test_audio_segment_mapping.py
├── test_dp_talt_shapes.py
├── test_transition_mil.py
├── test_zero_residual_equivalence.py
├── test_checkpoint_compatibility.py
├── test_transition_health.py
└── test_video_split_integrity.py
```

---

## 10.3 保持不变

首版不修改：

* CLIP/DFN Transformer层数；
* ResNet-50主干；
* 3尺度×5帧crop定义；
* BGI数学结构；
* SRAC尺度权重；
* CE和RAL定义；
* optimizer类型；
* 数据标签；
* FakeAVCeleb和LAV-DF零微调协议。

---

# 十一、关键类和函数设计

## 11.1 `LipFD.get_features`

建议接口：

```python
def get_features(
    self,
    x: torch.Tensor,
    *,
    return_temporal_aux: bool = False,
):
    ...
```

默认：

```python
global_feature = model.get_features(x)
```

行为与旧代码完全一致。

创新模式：

```python
global_feature, audio_aux = model.get_features(
    x,
    return_temporal_aux=True,
)
```

`audio_aux`建议使用dataclass或普通字典：

```python
{
    "audio_segments": Tensor[B, 5, Ca],
    "audio_global": Tensor[B, Cg],
    "audio_segment_mask": Tensor[B, 5],
    "segment_mode": "timestamp" | "relative",
}
```

禁止使用：

```python
self.last_audio_aux = ...
```

因为可变模块状态容易在DDP、compile和并发推理中造成错误。

---

## 11.2 `AudioSegmentExtractor`

```python
class AudioSegmentExtractor(nn.Module):
    def forward(
        self,
        patch_grid: torch.Tensor,
        overlap_weights: torch.Tensor | None = None,
    ) -> tuple[torch.Tensor, torch.Tensor]:
        ...
```

输出：

```text
audio_segments [B,5,C]
valid_mask     [B,5]
```

职责仅限于时间分段，不包含分类。

---

## 11.3 `DifferencePreservingTransitionEncoder`

```python
class DifferencePreservingTransitionEncoder(nn.Module):
    def forward(
        self,
        visual_regions: torch.Tensor,
        audio_segments: torch.Tensor,
        audio_global: torch.Tensor | None = None,
    ) -> tuple[torch.Tensor, torch.Tensor]:
        ...
```

输出：

```text
visual_transitions [B,4,d]
audio_transitions  [B,4,d]
```

开关：

```text
use_semantic_modulation
semantic_modulation_scale
```

---

## 11.4 `CrossModalTransitionMatcher`

```python
class CrossModalTransitionMatcher(nn.Module):
    def forward(
        self,
        visual_transitions: torch.Tensor,
        audio_transitions: torch.Tensor,
    ) -> dict[str, torch.Tensor]:
        ...
```

输出：

```python
{
    "match_logits": Tensor[B,4,4],
    "match_prob": Tensor[B,4,4],
    "aligned_audio": Tensor[B,4,d],
    "aligned_tokens": Tensor[B,4,d],
}
```

`match_temperature`固定或由验证集预先确定，不在测试阶段调节。

---

## 11.5 `TransitionEvidenceHead`

```python
class TransitionEvidenceHead(nn.Module):
    def forward(
        self,
        aligned_tokens: torch.Tensor,
    ) -> torch.Tensor:
        ...
```

输出：

```text
instance_logits [B,4]
```

---

## 11.6 `SparseTransitionMILPool`

```python
class SparseTransitionMILPool(nn.Module):
    def forward(
        self,
        instance_logits: torch.Tensor,
        valid_mask: torch.Tensor | None = None,
    ) -> torch.Tensor:
        ...
```

支持消融：

```text
mean
max
logsumexp
```

其中LogSumExp必须使用稳定实现：

```python
pooled = (
    tau * torch.logsumexp(instance_logits / tau, dim=-1)
    - tau * math.log(num_valid_instances)
)
```

---

## 11.7 `RegionAware.forward`

建议扩展：

```python
def forward(
    self,
    crops,
    global_feature,
    *,
    temporal_aux=None,
    return_aux=False,
):
    ...
```

关闭创新时仍返回：

```python
pred_score, weights_max, weights_org
```

开启并要求辅助结果时：

```python
pred_score, weights_max, weights_org, transition_aux
```

`transition_aux`只用于日志和可视化，不参与旧接口。

---

## 11.8 `Trainer._forward_impl`

伪代码：

```python
if self.opt.enable_dp_talt:
    global_feature, audio_aux = self.encoder.get_features(
        self.input,
        return_temporal_aux=True,
    )

    logits, weights_max, weights_org, transition_aux = self.model(
        self.crops,
        global_feature,
        temporal_aux=audio_aux,
        return_aux=True,
    )
else:
    global_feature = self.encoder.get_features(self.input)

    logits, weights_max, weights_org = self.model(
        self.crops,
        global_feature,
    )

loss_ce = self.criterion_ce(logits.float(), labels)
loss_ral = self.criterion_ral(weights_max.float(), weights_org.float())
loss = loss_ce + 0.01 * loss_ral
```

关闭创新时必须与原路径完全相同。

---

# 十二、建议配置参数

## 必需参数

```yaml
innovation:
  enable_dp_talt: false
  enable_transition_mil: false

  audio_segment_mode: relative
  transition_dim: 128
  use_semantic_modulation: false
  semantic_modulation_scale: 0.1

  match_temperature: 1.0
  transition_pool: logsumexp
  tau_transition: 1.0
  learnable_temperature: false

  alpha_mil_init: 0.0

  use_video_level_mil: false
  tau_video: 1.0

  log_transition_health: true
  transition_health_interval: 500
```

## 明确关闭

```yaml
  use_real_instance_loss: false
  use_shift_loss: false
  use_entropy_gate: false
  use_rd_rsec: false
```

---

# 十三、检查点和预训练权重兼容

## 13.1 旧checkpoint加载新模型

只允许以下新前缀缺失：

```text
transition_alignment.*
transition_evidence_head.*
transition_mil.*
alpha_mil
audio_segment_extractor.*
```

不允许使用广泛的：

```text
strict=False
```

后直接忽略所有缺失键。

必须显式检查：

* encoder主干键无缺失；
* ResNet键无缺失；
* 原分类头键无缺失；
* BGI/SRAC键无缺失；
* 只有新增模块键可以缺失。

---

## 13.2 关闭创新时的等价测试

固定：

* checkpoint；
* 输入batch；
* FP32；
* eval模式；
* dropout关闭。

要求：

```text
enable_dp_talt=False
enable_transition_mil=False
```

时新代码与旧代码输出logits、CE和RAL数值一致到合理浮点误差。

---

## 13.3 零初始化等价测试

开启创新但：

```text
alpha_mil = 0
```

时最终分类logits应与原模型一致。

DP-TALT内部可以计算，但不应改变输出。

---

# 十四、数据字段和数据审计

## 14.1 ST-MIL需要的数据

ST-MIL只需要当前已有：

```text
global image
crops
label
```

因此MVP不依赖video_id。

---

## 14.2 HS-TMIL新增字段

```python
{
    "sample_id": str,
    "video_id": str,
    "window_index": int,
    "window_start": float | None,
    "window_end": float | None,
    "label": int,
}
```

当前数据集主要返回`img, crops, label`，没有这些字段。

建议使用manifest，而不是在训练期间临时用正则猜测：

```csv
sample_path,split,video_id,window_index,window_start,window_end,label
```

---

## 14.3 数据审计输出

| 数据集         | Split | 视频数 | 窗口数 | 每视频窗口均值 | 中位数 | ≥2窗口比例 | 单窗口比例 | 跨Split重复 |
| ----------- | ----- | --: | --: | ------: | --: | -----: | ----: | -------- |
| AVLips      | Train |     |     |         |     |        |       |          |
| AVLips      | Val   |     |     |         |     |        |       |          |
| AVLips      | Test  |     |     |         |     |        |       |          |
| FakeAVCeleb | Test  |     |     |         |     |        |       |          |
| LAV-DF      | Test  |     |     |         |     |        |       |          |

若AVLips训练数据无法可靠构造多窗口Bag，最终名称使用ST-MIL。

---

# 十五、最小可行实现方案

## MVP-0：先修复评价基线

必须先完成：

1. 视频级验证；
2. 验证集选择best checkpoint；
3. 验证集确定分类阈值；
4. 固定视频聚合；
5. 测试阶段不重新选阈值或聚合；
6. deterministic与compile开关分离；
7. 固定数据manifest。

否则创新实验无效。

---

## MVP-1：只暴露五帧和五段特征

修改：

* `LipFD.get_features(return_temporal_aux=True)`；
* `region_awareness.forward(return_aux=True)`。

暂时不增加分类模块。

验证shape：

```text
audio_segments: [B,5,Ca]
visual_regions: [B,5,Cv]
```

---

## MVP-2：实现DP-TALT

只实现：

```text
signed transition
→ 4×4 cosine field
→ soft transfer
→ aligned tokens
```

不启用语义调制。

执行：

* shape测试；
* 梯度测试；
* audio shuffle；
* 匹配场可视化；
* 维度健康监控。

---

## MVP-3：实现ST-MIL

```text
aligned tokens
→ instance logits
→ tau=1.0 LSE
→ alpha_mil零初始化残差
```

不使用：

* 视频级Bag；
* 新损失；
* 可学习温度；
* 实例约束。

---

## MVP-4：奥卡姆对照

比较：

* 原MB-ViT；
* 等参数MLP；
* 1D-CNN；
* DP-TALT＋mean；
* DP-TALT＋ST-MIL。

3个配对seed短训练。

---

## MVP-5：条件化增强

只有前面通过后，依次尝试：

1. 有界深层语义调制；
2. 温度0.5/1.0/1.5；
3. 数据支持时启用HS-TMIL。

每次只增加一个变量。

---

# 十六、实验设计

## 16.1 消融矩阵

| 编号  | 模型              | 目的         |
| --- | --------------- | ---------- |
| B0  | MB-ViT          | 原始基线       |
| B1  | MB-ViT继续同预算训练   | 排除额外训练轮数   |
| B2  | 等参数MLP          | 排除参数量收益    |
| B3  | 轻量1D-CNN        | 奥卡姆时序基线    |
| B4  | DP-TALT＋mean    | 验证局部对齐表示   |
| B5  | DP-TALT＋max     | 验证硬实例选择    |
| B6  | DP-TALT＋ST-MIL  | 完整窗口级方案    |
| B7  | DP-TALT＋HS-TMIL | 条件化完整方案    |
| B8  | 固定同位置匹配         | 验证4×4软匹配   |
| B9  | audio shuffle   | 验证音频时序是否生效 |
| B10 | 去除语义调制          | 验证深层语义贡献   |
| B11 | alpha关闭         | 验证MIL实际贡献  |

---

## 16.2 指标

### 检测指标

* 视频级AUC；
* 视频级AP；
* ACC@验证阈值；
* FPR；
* FNR；
* Recall；
* 可选ECE和Brier。

### 稳定性指标

* 每个seed原始结果；
* mean±std；
* 中位数；
* 配对差值；
* 配对Bootstrap置信区间；
* 最差观察结果；
* 不把最差值称为理论下界。

### 机制指标

* `alpha_mil`；
* `alpha_mil.grad`；
* MIL实际logit贡献比例；
* 四个实例权重；
* 有效实例数；
* 匹配场熵；
* 匹配场Mean Max Probability；
* 有效匹配数；
* 非对角转移余弦；
* 特征有效秩；
* audio shuffle前后变化。

### 效率指标

* 新增参数量；
* FLOPs；
* `max_memory_allocated`；
* `max_memory_reserved`；
* samples/s；
* 单epoch时间；
* checkpoint大小；
* 推理窗口吞吐。

---

## 16.3 多随机种子方案

### 筛选阶段

3个配对seed：

```text
Baseline(seed_i)
1D-CNN(seed_i)
DP-TALT+ST-MIL(seed_i)
```

每个seed必须保持：

* 相同预训练权重；
* 相同数据顺序生成规则；
* 相同训练epoch；
* 相同optimizer update次数；
* 相同早停；
* 相同验证聚合。

### 正式阶段

建议使用6个新的配对seed：

```text
MB-ViT
最强简单基线
最终Full
```

报告全部单次结果，不根据单次最好值下结论。

---

# 十七、健康监控与风险控制

## 17.1 转移表示坍缩

记录：

```text
temporal_std_v
temporal_std_a
batch_std_v
batch_std_a
offdiag_cos_v
offdiag_cos_a
effective_rank_v
effective_rank_a
```

---

## 17.2 匹配场异常

记录：

```text
field_entropy
field_mean_max_prob
field_effective_matches
```

可能状态：

| 状态    | 典型表现         |
| ----- | ------------ |
| 均匀坍缩  | 高熵、最大概率接近均匀值 |
| 极端尖锐化 | 低熵、最大概率接近1   |
| 位置固定  | 不同样本匹配场几乎相同  |
| 正常软匹配 | 样本间变化明显、非恒定  |

不设置未经实验支持的固定阈值。

---

## 17.3 MIL分支无声失败

同时检查：

1. `alpha_mil.grad`；
2. `alpha_mil`变化；
3. DP-TALT内部参数梯度；
4. 实际logit贡献；
5. 推理时关闭分支的指标变化。

---

# 十八、通过条件、停止条件和回退方案

## 18.1 通过条件

### DP-TALT成立

至少满足：

1. 多seed下优于等参数MLP；
2. 不稳定弱于1D-CNN；
3. audio shuffle后匹配结构或检测结果显著退化；
4. 匹配场非恒定；
5. DP-TALT＋mean优于原始五帧均值或至少表现出局部困难样本改善；
6. FakeAVCeleb和LAV-DF不存在系统性下降。

### ST-MIL成立

至少满足：

1. LSE稳定优于普通mean；
2. 不像max一样明显放大误报；
3. 多seed方向一致；
4. 关闭MIL残差后性能下降；
5. 实例分数具有时间差异。

### HS-TMIL成立

还必须满足：

1. 无跨Split视频泄漏；
2. 训练集具备多窗口视频Bag；
3. 视频级训练优于ST-MIL；
4. 额外显存和时间可接受。

---

## 18.2 停止条件

出现以下任一，应停止当前层级：

* 无法验证音频patch时间方向；
* 五段音频特征只是同一个全局向量复制；
* audio shuffle几乎没有影响；
* 匹配场长期恒定；
* DP-TALT稳定弱于1D-CNN；
* ST-MIL不优于mean；
* 只有单个seed明显提高；
* AVLips提高但两个跨域数据集持续下降；
* HS-TMIL数据条件不成立；
* 视频级Bag导致明显OOM或训练预算不公平。

---

## 18.3 回退路径

```text
完整HS-TMIL失败
→ 保留ST-MIL

DP-TALT软匹配失败
→ 同位置音画匹配＋ST-MIL

DP-TALT不优于简单时序
→ 1D-CNN＋ST-MIL

ST-MIL不优于mean
→ 仅保留DP-TALT＋mean

全部失败
→ 保持原MB-ViT，不包装无效第二创新
```

---

# 十九、最终建议目录结构

```text
lip-AiStation/
├── models/
│   ├── LipFD.py
│   ├── region_awareness.py
│   ├── local_transition_alignment.py
│   ├── transition_mil.py
│   └── transition_outputs.py
├── data/
│   ├── datasets.py
│   ├── video_bag_sampler.py          # 条件新增
│   └── window_manifest.py
├── trainer/
│   └── trainer.py
├── options/
│   ├── base_options.py
│   └── train_options.py
├── utils/
│   ├── transition_health.py
│   └── video_aggregation.py
├── tools/
│   ├── audit_video_bags.py
│   ├── build_window_manifest.py
│   └── inspect_audio_patch_mapping.py
├── tests/
│   ├── test_audio_segment_mapping.py
│   ├── test_dp_talt_shapes.py
│   ├── test_transition_mil.py
│   ├── test_zero_residual_equivalence.py
│   ├── test_checkpoint_compatibility.py
│   ├── test_transition_health.py
│   └── test_video_split_integrity.py
├── train.py
├── validate.py
└── test.py
```

---

# 二十、最终结论

最终第二大创新点锁定为：

[
\boxed{
\text{DP-TALT}
+
\text{HS-TMIL / ST-MIL}
}
]

其中：

* **DP-TALT**解决“如何从现有五帧多尺度嘴部特征和局部音频特征中构造局部跨模态转移证据”；
* **ST-MIL**解决“如何避免窗口内少量异常转移被平均稀释”；
* **HS-TMIL**在数据条件允许时进一步解决“如何避免视频中少量异常窗口被视频均值稀释”。

该方案与现有MB-ViT的衔接位置明确：

```text
BGI提供全局音视频语义
RAE/SRAC提供五帧多尺度嘴部表示
DP-TALT构造局部音画转移证据
ST/HS-MIL完成稀疏证据聚合
```

它不修改现有主干，不增加新的大型编码器，不增加新人工标签，MVP也不增加新损失，具备较好的工程可实施性。

但仍有三个尚未闭合的前提：

1. 音频patch是否能够恢复可靠的五段时间顺序；
2. AVLips是否支持无泄漏的视频级多窗口Bag；
3. DP-TALT是否稳定优于1D-CNN等简单时序基线。

因此，在完成数据审计、评价协议修复、梯度冒烟测试和3-seed奥卡姆对照前，最准确的结论仍是：

> **该方案已具备进入代码实现和实验验证的条件，但有效性尚待实验验证，不能根据单次最好结果认定创新成立。**
